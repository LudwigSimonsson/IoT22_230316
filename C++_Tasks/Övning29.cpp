#include<iostream>

using namespace std;

void Armstrong(int n)
{
    int sum=0,r;
    int m=n;
    
    //write a loop to find sum of cubes of digits
    //check if a number is Armstrong
    
     if(sum==m)
        cout<<"armstrong";
    else
        cout<<"not armstrong";
}